services:
  - name: bigboss
    git:
      repo: "https://github.com/sahansandaruwan/bigboss"
      branch: master
    envs:
    - key: BUILD_ONLY_VAR
      scope: BUILD_TIME
      value: hello world
    - key: RUNTIME_ONLY_VAR
      scope: RUN_TIME
      value: i'm a little teapot
